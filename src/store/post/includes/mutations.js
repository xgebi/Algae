export default {
	setIncludeThemeCss(state, includeThemeCss) {
		state.includeThemeCss = includeThemeCss;
	},
	setCssContent(state, cssContent) {
		state.cssContent = cssContent;
	},
	setJsContent(state, jsContent) {
		state.jsContent = jsContent;
	},
	setThumbnailType(state, thumbnailType) {
		state.thumbnailType = thumbnailType;
	},
	setThumbnailContent(state, thumbnailContent) {
		state.thumbnailContent = thumbnailContent;
	},
}